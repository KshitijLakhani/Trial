#include <stdio.h>
#include <conio.h>

int ip;
printf("Enter the number you want to check for odd / even");
scanf("%d",&ip);
if(ip%2 == 0){
printf("%d is divisible by 2",ip);
}
else{
printf("%d is not divisible by 2",ip);
}
